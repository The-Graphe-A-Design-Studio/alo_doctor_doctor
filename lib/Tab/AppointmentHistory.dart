import 'package:flutter/material.dart';

class AppointmentTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      body: Center(
        child: Text('appointment Hstory'),
      ),
    );
  }
}
